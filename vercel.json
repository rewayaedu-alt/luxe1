{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "outputDirectory": "dist",
  "env": [
    "VITE_API_URL",
    "DATABASE_URL",
    "JWT_SECRET",
    "JWT_EXPIRY"
  ],
  "functions": {
    "api/**/*.js": {
      "memory": 1024,
      "maxDuration": 60
    }
  }
}
